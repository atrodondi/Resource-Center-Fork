const { Builder, By, until } = require("selenium-webdriver");
const assert = require("assert");

// icon needs name or some other kind of locator, different class prob

//add resource button needs a name

//logout button needs a name

module.exports = {
  // test
};
